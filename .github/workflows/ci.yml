name: ci

on:
  push:
    branches: ['**']
  pull_request:
    branches: [dev]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Lint
      run: make lint
    - name: Format
      run: make format
    - name: Build
      run: make build
